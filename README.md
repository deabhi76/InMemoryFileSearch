# InMemoryFileSearch
An in-memory C++ file search system that indexes local text files, supports phrase queries, ranks results using TF-IDF inspired scoring, and enables interactive document selection.
